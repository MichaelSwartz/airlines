<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <a href="<%= admin_user_path(user) %>"><%= user.email %></a>
      <% if user.admin %>
        Admin
      <% end %>
    </li>

  <% end %>
</ul>

<%= paginate @users %>
